name: test-snap
base: core22
version: '0.0.1'
summary: Test snap for BootStack actions
description: Test snap for BootStack actions

grade: devel
confinement: devmode

parts:
  test-snap:
    plugin: python
    python-requirements: [ ./requirements.txt ]
    source: .

